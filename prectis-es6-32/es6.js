/* const world = 'World'
const myPropertys = {
    name: 'Illizin Ruby',
    age: 103,
    phone: '+00978466387',
    address: 'Lower Bird',
    proffassion: 'programmar',
    person: 'Is Bad'
}
const { name, age, phone, address, proffassion, person } = myPropertys;

const mySimpleData = `Helow ${world} This is ${name}, my work experiance ${proffassion} and my age ${age}, and my habit ${person}`
console.log(mySimpleData); */

/* function dividing(div) {
    return div / 5;
}
console.log(dividing(100)); */
/* const dividing = div => div / 5;

console.log(dividing(200)); */
function multiply(mul1, mul2) {
    const result = mul1 + 2 * mul2 + 2;
    return result
}

// const multiply = mul, mul2 => mul + 2 mul2 + 2